# Deployment verification record

Date: 2026-09-04  
Revision: working tree for phases 8 through 12  
Environment: Linux, Node 26.8.1, Docker Engine 29.7.2, Docker Compose 5.5.0

This is a local verification record, not a production approval. `package.json`
requires Node 24, while this host has Node 26. The CI workflow uses Node 24.

| AC     | Verification method                                                                                                       | Status  | Evidence and required follow-up                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------ | ------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-001 | Run strict typecheck, lint, tests, worker build, and Next build from a clean checkout.                                    | PENDING | `npm run format:check`, `lint`, `typecheck`, `test:unit`, `test:coverage`, `build:worker`, and `build` all exited 0. Coverage was 97.2% statements and 93.5% branches. Repeat from a clean checkout on Node 24 because this run used Node 26.                                                                                                                                                                                             |
| AC-002 | Build runtime and migrator targets with BuildKit, then inspect startup command, UID, and image contents.                  | PENDING | `docker build --target runtime ...` could not use the Dockerfile cache mounts under the legacy builder. `DOCKER_BUILDKIT=1 docker build ...` then failed because buildx is missing. Install the Docker buildx component, run both target builds, run `id -u` in each image, and execute the image-policy checks from `.github/workflows/ci.yml`.                                                                                          |
| AC-003 | Run `docker compose --env-file .env config --quiet` with placeholder-free test values.                                    | PASS    | A temporary mode-0600 test environment was used. The command exited 0. The temporary file was removed.                                                                                                                                                                                                                                                                                                                                    |
| AC-004 | Start PostgreSQL 18 on a fresh named volume and wait up to 120 seconds for health.                                        | PENDING | PostgreSQL 18 started from a fresh disposable container and `pg_isready` passed. Repeat with the Compose named volume and inspect its mount before marking this complete.                                                                                                                                                                                                                                                                 |
| AC-005 | Apply production migrations from empty and previous-version databases, then repeat.                                       | PENDING | The Playwright setup applied `20260903230628_initial` to an empty PostgreSQL 18 database; a second deploy reported no pending migrations. The integration suite also passed fresh and repeat migration checks. Restore the previous release database, run `prisma migrate deploy`, and inspect constraints and indexes.                                                                                                                   |
| AC-006 | Start the migrated app, check the safe `/api/health` schema and latency, then make PostgreSQL unavailable and expect 503. | PENDING | The migrated app served the E2E suite. The repository smoke harness passed against its safe HTTP fixture. Start the Compose app, time `/api/health`, stop PostgreSQL without removing its volume, and verify a 503 response before restoring it.                                                                                                                                                                                          |
| AC-007 | Run automated state/error tests and a manual staging OAuth flow.                                                          | PENDING | Nineteen OAuth unit tests, nine provider tests, and the Playwright fake-provider login flow passed. Configure the exact staging callback in the Spotify Dashboard, complete login in staging, inspect logs for token absence, and test denied and replayed callbacks.                                                                                                                                                                     |
| AC-008 | Exercise access refresh and expired or revoked refresh tokens.                                                            | PASS    | Unit and PostgreSQL integration tests passed transparent refresh serialization, retained refresh-token behavior, and the `NEEDS_REAUTH` transition for `invalid_grant`.                                                                                                                                                                                                                                                                   |
| AC-009 | Run a recently-played fixture through synchronization and inspect event, metadata, cursor, and next-run state.            | PASS    | The PostgreSQL integration suite committed the 50-item fixture atomically, updated the cursor and audit row, and preserved the cursor on failure. Worker and unit builds passed.                                                                                                                                                                                                                                                          |
| AC-010 | Replay identical results sequentially and concurrently.                                                                   | PASS    | The PostgreSQL integration suite passed sequential replay and a two-worker race over the same 50 rows. The race produced exactly 50 rows, with no extras.                                                                                                                                                                                                                                                                                 |
| AC-011 | Verify dashboard data after sync, restart, and logout/login.                                                              | PENDING | Playwright passed empty and 55-row populated dashboard checks plus logout. Run the Compose restart test and log back in through staging OAuth to confirm the same totals and recent event.                                                                                                                                                                                                                                                |
| AC-012 | Run golden statistics across ranges, timezones, collaboration, DST, leap day, ties, and empty data.                       | PASS    | Thirty-two date tests and the PostgreSQL golden analytics fixture passed. The run covered DST boundaries, zero buckets, selected-year series, hour-by-weekday sums, deterministic rankings, and top-N plus Other denominators.                                                                                                                                                                                                            |
| AC-013 | Run cross-user API/search/export checks and CSRF rejection tests.                                                         | PASS    | Playwright returned 404 for a foreign track and 403 for unauthenticated-CSRF POST, PATCH, and DELETE requests. CSV formula neutralization passed through the authenticated export endpoint. Repository ownership tests also passed.                                                                                                                                                                                                       |
| AC-014 | Run deployment success and every specified failure fixture.                                                               | PENDING | All 21 Bats cases passed, including deployment failures and the new backup failure cases. Install buildx, start the full disposable Compose stack, and run `scripts/deploy.sh --skip-build --no-backup` through its success path.                                                                                                                                                                                                         |
| AC-015 | Restart containers and Docker with data present, then check lease recovery and dedupe.                                    | PENDING | Lease recovery and dedupe integration tests passed. Run the Compose restart-survival job, restart the Docker daemon, and compare the cursor and history counts before and after.                                                                                                                                                                                                                                                          |
| AC-016 | Create a real custom-format backup, restore it into a clean database, and run health and consistency checks.              | PENDING | Five backup Bats cases passed missing-Docker, safe-directory creation, unsafe path, `.failed` preservation, scoped retention, and secret-output checks. Run `scripts/backup.sh` against the disposable stack and follow `docs/runbooks/backup-restore.md` through the full restore drill.                                                                                                                                                 |
| AC-017 | Inspect consent, retention, export, disconnect, deletion, and metadata freshness behavior.                                | PASS    | Playwright passed consent, export, and logout. Integration tests passed finite retention and repeated disconnect. Metadata refresh fixture tests passed bounded, referenced-only updates without network access or an artwork cache.                                                                                                                                                                                                      |
| AC-018 | Run every release suite and CI job with no critical quarantine.                                                           | PENDING | Local lint, typecheck, 154 unit tests, coverage, 17 PostgreSQL integration tests, two smoke tests, 21 Bats tests, two Playwright tests, worker build, and Next build passed. The first Playwright attempt failed because its browser was absent; the final run passed with a locally installed Chromium 1234 executable. Run the full GitHub Actions workflow, including pinned Chromium, image, Compose, security, and performance jobs. |

## Other executed checks

- `npm run test:e2e` passed 2 tests against the loopback fake provider and a
  fresh PostgreSQL 18 database.
- `npm run test:smoke` passed 2 tests without starting OAuth or mutating data.
- `npm run test:shell` passed 21 tests. ShellCheck was not installed locally,
  so `npm run lint:shell` remains for CI or a host with ShellCheck.

README release-ready wording was not changed because AC-002 and other local
verification steps remain pending.
